<nav id="main-menu" class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <!-- Logo -->
        <section class="portfolio-experiment">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <span class="text">Emprunthèque</span>
                <span class="line -right"></span>
                <span class="line -top"></span>
                <span class="line -left"></span>
                <span class="line -bottom"></span>
            </a>
        </section>

        <!-- Bouton pour le menu mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars fa-2x text-primary"></i>
        </button>

        <!-- Menu principal -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">ACCUEIL</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_objet') }}">LES OBJETS</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_objet_create') }}">PUBLIER UN OBJET</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">CONTACT</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link " href="{{ path('admin') }}">ADMIN</a>
                </li>
                {% endif %}
            </ul>

            <!-- Espace Membre -->
            <ul class="navbar-nav d-flex align-items-center">
                {% if app.user %}
                    <!-- Menu déroulant utilisateur -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle btn btn-outline-info" href="#" id="navbarDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="far fa-address-card"></i> {{ app.user.prenom}} {{ app.user.nom}}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_user') }}">MON PROFIL</a></li>
                            <li><a class="dropdown-item" href="#">MODIFIER MON PROFIL</a></li>
                            <li><a class="dropdown-item" href="#">MODIFIER MON MOT DE PASSE</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">DÉCONNEXION</a></li>
                        </ul>
                    </li>
                {% else %}
                    <!-- Boutons Connexion & Inscription -->
                    <li class="nav-item ms-2">
                        <a class="nav-link btn btn-outline-primary" href="{{ path('app_login') }}">
                            <i class="fas fa-user"></i> Se Connecter
                        </a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="nav-link btn btn-outline-success" href="{{ path('app_register') }}">
                            <i class="fas fa-registered"></i> Créer un compte
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>


